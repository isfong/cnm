apiVersion: v1
kind: Service
metadata:
  name: {{ include "example-inventory-service.fullname" . }}
  labels:
    {{- include "example-inventory-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "example-inventory-service.selectorLabels" . | nindent 4 }}
