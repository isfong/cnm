apiVersion: v1
kind: Service
metadata:
  name: {{ include "example-product-service.fullname" . }}
  labels:
    {{- include "example-product-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "example-product-service.selectorLabels" . | nindent 4 }}
