apiVersion: v1
kind: Service
metadata:
  name: {{ include "example-order-service.fullname" . }}
  labels:
    {{- include "example-order-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "example-order-service.selectorLabels" . | nindent 4 }}
